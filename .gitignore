.DS_Store
.env
.env.*

# council runtime data
.council/
.claude/
.headroom/
poc/

# Internal planning notes — not part of the public release
PLAN-*.md

# Build outputs
bin/
dist/
coverage.out

# JavaScript examples
node_modules/
*.tsbuildinfo

# 3D source model — bake input only (attribution in NOTICE). The committed,
# decimated cloud is internal/tui/anim/mesh_data.go (see meshgen.go).
internal/tui/anim/testdata/
